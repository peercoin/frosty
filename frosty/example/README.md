# Frosty Flutter example

Demonstrates using the `frosty` package directly from Flutter. The package's
build hook compiles and bundles the Rust library for native targets.

There is also `testnet_example.dart` and `tweak_example.dart` showing usage.
